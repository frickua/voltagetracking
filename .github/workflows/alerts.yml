name: alerts.yml
on:
  workflow_dispatch:
  repository_dispatch:
    types: [ grafana-alert ]
jobs:
  send-alerts:
    #TODO: prepare own docker image to speedup startup
    runs-on: ubuntu-latest
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
      GRAFANA_URL: ${{ secrets.GRAFANA_URL }}
      GRAFANA_TOKEN: ${{ secrets.GRAFANA_TOKEN }}
      TZ: "Europe/Kyiv"
    steps:
      - name: Debug output
        run: |
            echo '${{ toJson(github.event.client_payload) }}'
#      - name: Checkout repository
#        uses: actions/checkout@v6.0.2
#
#      - name: Set up Python
#        uses: actions/setup-python@v6.2.0
#        with:
#          python-version: '3.14'
#
#      - name: Install dependencies
#        run: |
#          python -m pip install --upgrade pip
#          if [ -f py-scripts/alerts/requirements.txt ]; then pip install -r alerts/requirements.txt; fi
#
#      - name: Run Chart processing script
#        run: python py-scripts/alerts/alerts.py

